//! Query module for `advisories`, `affected_ranges`, and `affected_versions`.
//! Includes the Phase 1 narrow query for the matcher (DESIGN §8.4) and the
//! per-ecosystem atomic replacement used by sync (DESIGN §8.6). Bodies land
//! with **M2-13** and **M2-16**.
