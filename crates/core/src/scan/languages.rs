//! Language statistics (FR-2.1, FR-2.2) via `tokei` as a library, honoring
//! the prune list. Aggregated into `repo_languages` with percentages; the 2%
//! threshold applies to the primary list while the full breakdown is
//! retained. Implemented in **M1-4**.
