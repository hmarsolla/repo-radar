//! OSV record JSON subset, `classify()` (Compromise vs Vulnerability), and
//! `has_malicious_marker` — deliberately one small function so the M2-12
//! spike's findings change exactly one place (DESIGN §8.2). Implemented in **M2-11**.
